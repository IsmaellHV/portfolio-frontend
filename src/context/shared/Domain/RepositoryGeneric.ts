 
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export interface RepositoryGeneric<T = unknown> {
  // Interface genérica para repositorios
  // El tipo T se puede usar en implementaciones específicas
}
