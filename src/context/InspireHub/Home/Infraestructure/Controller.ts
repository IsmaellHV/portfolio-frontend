import { PropsView } from '../Domain/PropsView';

export const Controller = (): PropsView => {
  //#region INICIALITATION
  const init = async () => {};

  const end = async () => {};
  //#endregion

  //#region EXPORT
  return {
    end,
    init,
  };
  //#endregion
};
